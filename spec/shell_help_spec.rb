require 'spec_helper'

describe ShellHelp do
  it 'has a version number' do
    expect(ShellHelp::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
